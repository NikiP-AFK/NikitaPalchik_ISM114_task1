#include "math_healper.h"
#include <cmath>

double pif_helper(const double a, const double b){
    return abs(sqrt(a**2 + b**2));
}

double sub_helper(const double a, const double b){
    return a-b;
}
